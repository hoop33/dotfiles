# No plugins registered. Modern Nushell manages plugins via `plugin add
# <path>` (writes to plugin.msgpackz), not by hand-editing this file with
# `register` calls -- that command doesn't exist anymore in 0.112.
